# Changelog

All notable changes to this package are documented in this file.

## [1.0.0] - 2026-08-16

### Added

- Initial release: `EventService` with `AddEventListener`, `RemoveEventListener` and `DispatchEvent`, backed by `UnityEngine.Events.UnityEvent<T>`.
- `GameEvent` base class and `IEventService` abstraction.
